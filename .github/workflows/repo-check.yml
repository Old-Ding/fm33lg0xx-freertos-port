name: Repository checks

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  repo-check:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run repository checks
        shell: pwsh
        run: .\scripts\check-repo.ps1
